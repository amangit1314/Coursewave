// src/types/express/index.d.ts

declare namespace Express {
  export interface Request {
    user?: {
      id: string;
      // Add other user properties here as your application grows
      email?: string;
      name?: string;
    };
    // If you have other custom properties, like 'project' in the previous example,
    // declare them here as well.
    project?: {
      id: string;
      // ...
    };
  }
}