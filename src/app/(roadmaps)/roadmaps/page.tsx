import React, { Suspense } from "react";

const RoadMapsPage = () => {
  return (
    <div className="flex h-full">
      <Suspense>RoadMaps</Suspense>
    </div>
  );
};

export default RoadMapsPage;
