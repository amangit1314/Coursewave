                <li className="mr-2">

                    <a
                        href="#"
                        onClick={() => setActiveTab(index)}
                        className={`inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white ${activeTab === index
                                ? 'text-white bg-blue-600'
                                : 'text-gray-500'
                            }`}
                        aria-current={activeTab === index ? 'page' : null}
                    >
                        {tab.title}
                    </a>

                    {/* <a href="#" className="inline-block px-6 py-2 text-white bg-blue-600 rounded-full active" aria-current="page">Tab 1</a> */}
                </li>
                <li className="mr-2">
                    <a
                        href="#"
                        onClick={() => setActiveTab(index)}
                        className={`inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white ${activeTab === index
                                ? 'text-white bg-blue-600'
                                : 'text-gray-500'
                            }`}
                        aria-current={activeTab === index ? 'page' : null}
                    >
                        {tab.title}
                    </a>

                    {/* <a href="#" className="inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white">Tab 2</a> */}
                </li>
                <li className="mr-2">
                    <a
                        href="#"
                        onClick={() => setActiveTab(index)}
                        className={`inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white ${activeTab === index
                                ? 'text-white bg-blue-600'
                                : 'text-gray-500'
                            }`}
                        aria-current={activeTab === index ? 'page' : null}
                    >
                        {tab.title}
                    </a>

                    {/* <a href="#" className="inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white">Tab 3</a> */}
                </li>
                <li className="mr-2">
                    <a
                        href="#"
                        onClick={() => setActiveTab(index)}
                        className={`inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white ${activeTab === index
                                ? 'text-white bg-blue-600'
                                : 'text-gray-500'
                            }`}
                        aria-current={activeTab === index ? 'page' : null}
                    >
                        {tab.title}
                    </a>

                    {/* <a href="#" className="inline-block px-6 py-2 rounded-full hover:text-gray-900 hover:bg-gray-100 dark:hover:bg-gray-800 dark:hover:text-white">Tab 4</a> */}
                </li>
                {/* <li>
                    <a className="inline-block px-4 py-3 text-gray-400 cursor-not-allowed dark:text-gray-500">Tab 5</a>
                </li> */}